'use strict';

require(['./board'], function( BoardJS ){

	describe('Matrix', function() {
		
		it('should create new matrix', function() {
			expect(1).toBe(1);
		});

		it('should create new matrix 1', function() {
			expect(1).toBe(1);
			expect(1).toBe(1);
		});
	});

});